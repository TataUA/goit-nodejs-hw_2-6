const {
  addContactValidate,
  updateContactFavoriteValidate,
} = require("./contact-validation");
const isValidId = require("./isValidId");

module.exports = {
  addContactValidate,
  updateContactFavoriteValidate,
  isValidId,
};
